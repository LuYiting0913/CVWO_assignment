<h1>Show Student</h1>

<%= @students.name %><br>
<%= @students.matric %><br>
<%= @students.gender %><br>

<%= link_to  "Edit", edit_student_path(@students) %>
<%= link_to  "Delete", student_path(@students),
              method: :delete,
              data: {confirm: "Confirm Deletion?"} %>
<br>



<%= link_to  "View Exam Results", student_grades_url(@students) %>



<%= link_to  "back", root_path %>
